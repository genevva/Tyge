{
  "name": "hai-ts",
  "version": "2.0.0",
  "description": "Claude Messages API Gateway - Bun Edition",
  "type": "module",
  "scripts": {
    "dev": "bun --hot server.ts",
    "start": "bun server.ts",
    "install-sdk": "bun add @anthropic-ai/claude-agent-sdk express cors",
    "install-types": "bun add -d @types/express @types/cors"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "express": "^4.19.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^22.0.0"
  }
}
